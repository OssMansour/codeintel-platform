# =============================================================================
# CodeIntel Platform -- Python Requirements (pinned for reproducible builds)
# =============================================================================
# Regenerate lock: pip-compile requirements.txt --output-file requirements.lock

# --- Web Framework ---
fastapi==0.115.6
gunicorn==22.0.0
uvicorn[standard]==0.32.1
pydantic==2.10.4
pydantic-settings==2.7.1

# --- LangGraph / LangChain ---
langgraph==0.2.60
langgraph-checkpoint-sqlite==2.0.1
langchain-core==0.3.28
langchain-ollama==0.2.2
langchain-community==0.3.13

# --- LLM Clients (core: Ollama + llama.cpp via OpenAI-compatible API) ---
openai==1.58.1

# --- LLM Clients (optional: install when switching provider) ---
# pip install langchain-openai       # for llama.cpp LangGraph or Azure OpenAI
# pip install boto3                   # for Amazon Bedrock
# pip install langchain-aws           # for Bedrock LangGraph integration

# --- Vector Database ---
qdrant-client==1.12.1

# --- Embedding & Reranking ---
sentence-transformers==3.3.1
torch==2.5.1
numpy==1.26.4
einops==0.8.0

# --- Tree-sitter Parsing ---
tree-sitter==0.23.2
# Language grammars -- install as needed:
# tree-sitter-python
# tree-sitter-javascript
# tree-sitter-typescript
# tree-sitter-go
# tree-sitter-java
# tree-sitter-c
# tree-sitter-cpp
# tree-sitter-c-sharp
# tree-sitter-kotlin
# tree-sitter-php
# Or use the bundled package:
tree-sitter-languages==1.10.2

# --- Async Tasks ---
celery[redis]==5.4.0
redis==5.2.1
kombu==5.4.2

# --- GitLab Integration ---
python-gitlab==4.13.0

# --- GitHub Integration ---
PyGithub==2.5.0

# --- Utilities ---
structlog==24.4.0
networkx==3.4.2
tqdm==4.67.1
python-multipart==0.0.18

# --- HTTP Client (for benchmarks & latency profiling) ---
httpx==0.28.1

# --- Optional: ONNX acceleration for CPU embeddings ---
# optimum[onnxruntime]>=1.20.0
